# API for ipmisc package

## Exported functions

long_to_wide_converter(data, x, y, subject.id = NULL, paired = TRUE, spread = TRUE, ...)
set_cwd()
signif_column(data, p, ...)
specify_decimal_p(x, k = 3L, p.value = FALSE)
stats_type_switch(type)

## Reexported objects

magrittr::%$%
zeallot::%<-%
magrittr::%<>%
magrittr::%>%
rlang::%|%
rlang::%||%
rlang:::=
tibble::as_tibble
tibble::enframe
tibble::tibble
